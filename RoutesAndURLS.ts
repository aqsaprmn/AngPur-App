export const BASE_URL = {

  base_50166:
    process.env.NODE_ENV === "production"
      ? "https://lrtosis.com:8080"
      : "/teeareport",
  base_50206:
    process.env.NODE_ENV === "production"
      ? "https://lrtosis.com:8080"
      : "/esoreport",
};
